# Copy to .env on the VPS and fill in. NEVER commit .env.
# Generate a secret with: openssl rand -hex 32
CAPTURE_TOKEN=replace-with-32-byte-hex-secret

# Optional comma-separated uid allow-list. Leave unset to allow any uid.
# OMI_ALLOWED_UIDS=uid1,uid2
